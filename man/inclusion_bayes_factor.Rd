% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inclusion_bayes_factor.R
\name{inclusion_bayes_factor}
\alias{inclusion_bayes_factor}
\title{Per-edge inclusion Bayes factors for a bgm() fit}
\usage{
inclusion_bayes_factor(fit, iter = 2000L, warmup = 500L, recompute = FALSE)
}
\arguments{
\item{fit}{A \code{bgms} fit from \code{bgm()} with \code{edge_selection = TRUE}
on continuous data (GGM).}

\item{iter}{Integer. Number of post-warmup iterations for the
prior-only chain. Default \code{2000}.}

\item{warmup}{Integer. Warmup length for the prior-only chain.
Default \code{500}.}

\item{recompute}{Logical. If \code{TRUE}, recompute the prior PIPs even
if a cached result is present. Default \code{FALSE}.}
}
\value{
A data frame with one row per potential off-diagonal edge
and columns
\describe{
\item{\code{edge}}{Character. Edge name (e.g. \code{"V1-V2"}).}
\item{\code{posterior_inclusion}}{Numeric. Posterior PIP from the
fitted chain.}
\item{\code{prior_inclusion}}{Numeric. Empirical prior PIP from the
prior-only chain.}
\item{\code{bf}}{Numeric. \code{(post / (1-post)) / (prior / (1-prior))}.
\code{Inf} when \code{posterior -> 1} with \code{prior < 1}.}
}
}
\description{
For a GGM fit run with \code{edge_selection = TRUE}, returns the
posterior inclusion probability, the empirical prior inclusion
probability, and the inclusion Bayes factor for each potential edge.
The prior PIP is estimated from a short prior-only chain that
samples from the joint prior on \verb{(K, Gamma)} using the same \code{V_ij}
as the data fit. The resulting BF is the \emph{joint-specification} BF
(ratio of posterior odds to prior odds, both conditional on
\code{V_ij(Y)}); it is consistent across interaction-prior families
because both PIPs come from the same chain machinery.
}
\details{
Results are cached on \code{fit$cache} so repeated calls are free unless
\code{recompute = TRUE}.
}
\examples{
\dontrun{
fit <- bgm(X, variable_type = "continuous",
           interaction_prior = graphical_g_prior(),
           edge_selection = TRUE)
inclusion_bayes_factor(fit)
}

}
