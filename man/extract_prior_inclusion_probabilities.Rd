% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_prior_inclusion_probabilities.R
\name{extract_prior_inclusion_probabilities}
\alias{extract_prior_inclusion_probabilities}
\title{Extract Prior Inclusion Probabilities}
\usage{
extract_prior_inclusion_probabilities(
  bgms_object,
  iter = 2000L,
  warmup = 500L,
  recompute = FALSE
)
}
\arguments{
\item{bgms_object}{A fitted model object of class \code{bgms} from
\code{\link[=bgm]{bgm()}} run with \code{edge_selection = TRUE}.}

\item{iter}{Integer. Number of post-warmup iterations for the
prior-only chain. Used only when simulation is required (joint-spec
Graphical G-prior). Default \code{2000}.}

\item{warmup}{Integer. Warmup length for the prior-only chain.
Default \code{500}.}

\item{recompute}{Logical. If \code{TRUE}, recompute even if a cached
result is present. Default \code{FALSE}.}
}
\value{
A symmetric p x p matrix of prior inclusion probabilities,
with variable names as row and column names. Diagonal entries are
\code{NA}. For non-Graphical-G priors all off-diagonal entries share
the same value (the edge-prior marginal).
}
\description{
Returns the prior inclusion probabilities for the edges of a model
fitted with \code{\link[=bgm]{bgm()}} (currently GGM only). The returned matrix has
the same shape and orientation as
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}} so the two can be
combined element-wise.

For most interaction priors the prior PIP is constant across edges
and read analytically from the edge prior:
\itemize{
\item Bernoulli(p): prior PIP = p
\item Beta-Bernoulli(alpha, beta): prior PIP = alpha / (alpha + beta)
}

For the joint-specification Graphical G-prior the marginal
P(gamma_ij = 1) depends on the data-defined V_ij and is not
analytic; it is estimated by running a short prior-only chain that
samples (K, Gamma) under the same V_ij as the data fit. The estimate
is cached on the fit so subsequent calls are free; pass
\code{recompute = TRUE} to re-run.
}
\seealso{
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}}, \code{\link[=bgm]{bgm()}}

Other extractors:
\code{\link[=extract_arguments]{extract_arguments()}},
\code{\link[=extract_category_thresholds]{extract_category_thresholds()}},
\code{\link[=extract_ess]{extract_ess()}},
\code{\link[=extract_group_params]{extract_group_params()}},
\code{\link[=extract_indicator_priors]{extract_indicator_priors()}},
\code{\link[=extract_indicators]{extract_indicators()}},
\code{\link[=extract_log_odds]{extract_log_odds()}},
\code{\link[=extract_main_effects]{extract_main_effects()}},
\code{\link[=extract_pairwise_interactions]{extract_pairwise_interactions()}},
\code{\link[=extract_partial_correlations]{extract_partial_correlations()}},
\code{\link[=extract_posterior_inclusion_probabilities]{extract_posterior_inclusion_probabilities()}},
\code{\link[=extract_precision]{extract_precision()}},
\code{\link[=extract_rhat]{extract_rhat()}},
\code{\link[=extract_sbm]{extract_sbm()}}
}
\concept{extractors}
